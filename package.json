{
  "scripts": {
    "start": "node app.js",
    "deploy": "git commit -am 'messagem padrao' && git push heroku master"
  },
  "dependencies": {
    "botbuilder": "^3.5.0-rc9",
    "nodemailer": "^0.7.1",
    "request": "^2.72.0"
  }
}
